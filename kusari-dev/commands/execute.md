---
description: Execute a single implementation plan step using the execute agent
argument-hint: <path-or-filename>
disable-model-invocation: false
---

Launch the `execute` agent with `$ARGUMENTS` as the step file path.
